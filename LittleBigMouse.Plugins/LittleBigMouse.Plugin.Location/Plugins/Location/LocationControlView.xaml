<UserControl x:Class="LittleBigMouse.Plugin.Location.Plugins.Location.LocationControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:location="clr-namespace:LittleBigMouse.Plugin.Location.Plugins.Location" xmlns:icons="clr-namespace:HLab.Icons.Wpf.Icons;assembly=HLab.Icons.Wpf" xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=location:LocationControlViewModel, IsDesignTimeCreatable=False}">
    <Grid>
        <StackPanel  Orientation="Horizontal" Height="Auto" HorizontalAlignment="Right" >
            <!--<ScrollViewer HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Disabled">-->
            <StackPanel Margin="5" Orientation="Horizontal" Height="Auto" HorizontalAlignment="Right">
                <!--<StackPanel Orientation="Vertical" Margin="5,0,0,0" VerticalAlignment="Center">
                    <CheckBox  Content="家庭影院" IsChecked="{Binding Path=Config.HomeCinema, Mode=TwoWay}" Foreground="LightGray"/>
                </StackPanel>-->
                <StackPanel Orientation="Vertical" Margin="5,0,0,0" VerticalAlignment="Center">
                    <mah:ToggleSwitch Content="水平循环" IsEnabled="{Binding Path=Layout.LoopAllowed}" IsOn="{Binding Path=Layout.LoopX, Mode=TwoWay}"/>
                    <mah:ToggleSwitch  Content="垂直循环" IsEnabled="{Binding Path=Layout.LoopAllowed}" IsOn="{Binding Path=Layout.LoopY, Mode=TwoWay}"/>
                    <!--<CheckBox   Content="显示标尺" IsChecked="{Binding Path=ShowRulers, Mode=TwoWay}" Foreground="LightGray" />-->
                </StackPanel>
                
                <StackPanel Orientation="Vertical" Margin="5,0,0,0" VerticalAlignment="Center">
                    <mah:ToggleSwitch  Content="允许重叠" IsOn="{Binding Path=Layout.AllowOverlaps, Mode=TwoWay}"/>
                    <mah:ToggleSwitch  Content="允许不连续" IsOn="{Binding Path=Layout.AllowDiscontinuity, Mode=TwoWay}"/>
                    <!--<CheckBox   Content="显示标尺" IsChecked="{Binding Path=ShowRulers, Mode=TwoWay}" Foreground="LightGray" />-->
                </StackPanel>
                
                <StackPanel Orientation="Vertical" Margin="5,0,0,0" VerticalAlignment="Center">
                    <mah:ToggleSwitch   Content="调整速度" IsEnabled="{Binding Path=Layout.AdjustSpeedAllowed}"  IsOn="{Binding Path=Layout.AdjustSpeed, Mode=TwoWay}"/>
                    <mah:ToggleSwitch   Content="调整指针" IsEnabled="{Binding Path=Layout.AdjustPointerAllowed}" IsOn="{Binding Path=Layout.AdjustPointer, Mode=TwoWay}"/>
                    <mah:ToggleSwitch   Content="允许拐角穿越" IsOn="{Binding Path=Layout.AllowCornerCrossing, Mode=TwoWay}"/>
                </StackPanel>
                
                <StackPanel Orientation="Vertical" Margin="5,0,0,0" VerticalAlignment="Center">
                    <mah:ToggleSwitch   Content="自动检查更新"  IsOn="{Binding Path=Layout.AutoUpdate, Mode=TwoWay}"/>
                    <mah:ToggleSwitch   Content="开机自启"  IsOn="{Binding Path=Layout.LoadAtStartup, Mode=TwoWay}"/>
                    <mah:ToggleSwitch   Content="启用" IsOn="{Binding Path=Layout.Enabled, Mode=TwoWay}"/>
                </StackPanel>
                
            </StackPanel>
            
            <Button MinWidth="50" Margin="5,0,0,0" Command="{Binding CopyCommand}" ToolTip="Copy config to clipboard">
                <icons:IconView Path="Icon/CopyConfig"/>
            </Button>
            
            <Button MinWidth="50" Margin="5,0,0,0" Command="{Binding StartCommand}" ToolTip="Apply/Start">
                <icons:IconView Path="Icon/Start"/>
            </Button>
            
            <Button MinWidth="50" Margin="5,0,0,0" Command="{Binding StopCommand}" ToolTip="Stop">
                <icons:IconView Path="Icon/Stop"/>
            </Button>
            
            <Button MinWidth="50" Margin="5,0,0,0" Command="{Binding UndoCommand}" ToolTip="Undo">
                <icons:IconView Height="20" Path="Icon/Undo"/>
            </Button>
        </StackPanel>

    </Grid>
</UserControl>
